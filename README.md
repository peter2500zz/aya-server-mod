# aya-server-mod

一个纯服务端的 Fabric 工具性指令模组，为 Minecraft 服务器提供若干实用命令。

## 环境

| 项目 | 版本 |
|------|------|
| Minecraft | 26.1（Mojang 官方映射） |
| Fabric Loader | 0.19.3 |
| Fabric API | 0.145.1+26.1 |
| Java | 25 |

模组功能逻辑全部作用于服务端，但 `environment` 设为 `"*"`，因此在单人、局域网（集成服务器）以及专用服务器中均可正常加载。

> 命令向玩家发送的提示文本复用 Minecraft 原版的翻译键，由客户端本地渲染。因此**客户端无需安装本模组**即可正确显示提示，并自动跟随客户端自身的语言设置。

## 指令列表

| 指令 | 参数 | 执行者 | 说明 |
|------|------|--------|------|
| `/here` | 无 | 仅玩家 | 对自身施加 30 秒发光效果（信标风格边框，无 HUD 图标），便于队友定位。 |
| `/tpa <玩家>` | 目标玩家 | 仅玩家 | 将自己立即传送至目标玩家位置，支持跨维度。补全体验与原版 `/tell` 一致（在线玩家名 + 选择器）。传送后分别私信通知双方（目标为自己时不重复提醒）。 |
| `/die` | 无 | 仅玩家 | 杀死自己，效果等同于 `kill @s`。 |
| `/hat` | 无 | 仅玩家 | 将主手物品与头部装备槽物品直接对调，无视任何条件（任意物品都能戴在头上）。 |
| `/back` | 无 | 仅玩家 | 传送回上一次死亡地点（支持跨维度）。无死亡记录或死亡维度已不存在时命令无效。 |
| `/home` | 无 | 仅玩家 | 传送回设置的重生点（床 / 重生锚，支持跨维度），复用原版复活坐标查找逻辑且不消耗重生锚充能。未设置重生点或床/锚被破坏、遮挡时提示并取消。 |

## 构建

```bash
./gradlew build
```

构建产物位于 `build/libs/` 目录下，将其中的主 JAR 放入服务器的 `mods/` 目录即可。

## 开发规范

本仓库的开发规范（映射规范、命令编写规范、文本输出规范、注释规范、Git 规范等）详见 [`CLAUDE.md`](CLAUDE.md)。

## 许可证

本项目以 CC0-1.0 许可证发布。
